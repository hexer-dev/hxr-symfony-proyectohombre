                    {{ form_start(form) }}
                        {% if form.person is defined %}
                            <div class="form-floating mb-3">
                                {{ form_widget(form.person, {'attr': {'class': 'select2 form-select'}}) }}
                                {{ form_label(form.person) }}
                                <small class="form-control-feedback text-danger">
                                    {{ form_errors(form.person) }}
                                </small>
                            </div>
                        {% endif %}
                        {% if form.program is defined %}
                            <div class="form-floating mb-3">
                                {{ form_widget(form.program, {'attr': {'class': 'select2 form-select'}}) }}
                                {{ form_label(form.program) }}
                                <small class="form-control-feedback text-danger">
                                    {{ form_errors(form.program) }}
                                </small>
                            </div>
                        {% endif %}
                        <div class="mb-3 form-check mr-sm-2">       
                            {{ form_widget(form.reductIrpf, {'attr': {'class': 'form-check-input'}}) }}                
                            {{ form_label(form.reductIrpf) }}                
                            {{ form_errors(form.reductIrpf) }}
                        </div>
                        {% if form.urlReturn is defined %}
                            {{ form_widget(form.urlReturn, {'attr': {'class': 'form-control'}}) }}                
                        {% endif %}
                        <div class="d-md-flex align-items-center">                      
                            <div class="mt-3 mt-md-0 ms-auto">
                                {{  form_row(form.save, {'attr': {'class': 'btn btn-info font-weight-medium rounded-pill px-4'}}) }}
                            </div>
                        </div>
                    {{ form_end(form) }}